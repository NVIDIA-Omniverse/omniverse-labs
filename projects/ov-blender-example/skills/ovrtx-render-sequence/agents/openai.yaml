interface:
  display_name: "OVRTX Render Sequence"
  short_description: "Develop stable native multi-frame OVRTX capture"
  default_prompt: "Use $ovrtx-render-sequence to implement or validate contiguous native OVRTX frames with stable session, sample, product, and frame identity."
