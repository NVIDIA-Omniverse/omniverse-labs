interface:
  display_name: "NanoUSD Gaussian Real-to-Sim"
  short_description: "Build interactive robotics scenes from Gaussian splats"
  default_prompt: "Use $nanousd-real-to-sim to ingest this Gaussian asset, create registered selections and colliders, fit/sweep articulations, and keep generated hidden geometry explicit. For every articulated front, use measured-only closed/half/open evidence before verification; report source, live-budget, measured-LOD0, and generated counts separately. Before promotion, obtain an artifact-only fresh-context reviewer verdict or explicitly mark it pending."
